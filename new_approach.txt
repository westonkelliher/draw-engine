We're going to take a totally new approach where the functions of our draw engine are for registering and manipulating draw objects. The draw objects will be things like rectangles, circles, etc. and they will have things like you will be able to out-draw an outline on the rectangle, you'll be able to rotate it, translate it, change the transform in other ways, and then you will also have draw object groups. The draw object group just has its own transform and then it has sub objects underneath it, And then the transform of the group is also applied to each object in the group. So you can compose things and rotate them altogether or translate them altogether, etc. <this is a raw, unedited speech-to-text transcript that may have errors>


Instead of having a display call which actually makes graphic calls to a buffer we're going to use a different approach which makes this more modular and makes it easier to test. We will have a render to draws function which returns a list of of draw calls that will ultimately be utilized by WGBU and we need to choose an appropriate format or representation for the list of draw calls. <this is a raw, unedited speech-to-text transcript that may have errors>
